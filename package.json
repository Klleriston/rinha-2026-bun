{
  "name": "rinha-2026-bun",
  "version": "1.0.0",
  "description": "Rinha de Backend 2026 — detecção de fraude com KNN in-memory (Bun + Elysia)",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test",
    "convert": "bun run convert.ts",
    "validate": "bun run validate.ts"
  },
  "dependencies": {
    "elysia": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
